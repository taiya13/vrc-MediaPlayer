# STATUS — 今どうなっているか

**このファイルの役目:** チャットが長くなって要約されたり、新しいチャットになったりしても、
「今どこまで進んでいて、何を、なぜ決めたか」をここを読めば追いつけるようにする。
振る舞いの決まりごと(いつも守ること)は [CLAUDE.md](../CLAUDE.md) を見てください。

このファイルは**都度書き換える前提**です。決定が変わったら、ここも直してください
(過去の経緯を消さず、下の「決定の記録」に残しつつ、上の「今の状態」を書き換える形を想定)。

作業中のタスク一覧は Claude Code のタスクツール(TaskList)にもありますが、
セッションをまたぐと見えなくなることがあるので、**このファイルが正**です。
新しいセッションでタスクツールを使うときは、ここの内容で作り直してください。

---

## 今の状態(2026-09-26 時点)

Phase8-3 まで完了。直近でやったのは:

1. **重ねない方式のフェード**(`TrackFadeModel` / `UdonTrackFader`)
   曲の終わりで音量を下げ、次の曲を0から上げる。2系統の動画プレイヤーを同時に鳴らして混ぜる
   従来のクロスフェード(`UdonCrossfadeCoordinator`, `UseCrossfade = false` で現在無効)はやめた。
   理由: 動画プレイヤーが2つ・音の出口が2つになることで壊れやすく、実際に機能しなかった。
2. **ミラー内の映像反転**(`SmartMediaVideoScreen.shader`)
   VRChatの `_VRChatMirrorMode` を見て、ミラーの中だけ映像を左右反転。字幕が鏡越しでも読めるように。
3. **名前付きプレイリスト**(`PlaylistShelfModel` / `UdonPlaylistShelf`)
   いま鳴っている曲+再生予定を、名前を付けて保存(最大20本・1本64曲)。
   VRChatの PlayerData にその人の分だけ保存(SDK 3.7.0以上が必要)。曲は番号でなくIDで覚える
   (カタログを作り直しても壊れないように)。

検査ツールが1つ増えた: `tools/typecheck/behaviour_sim/run.py`
(U#の実物を組み合わせて、保存→入り直し→再生→削除、のような操作の流れを実際に動かして確認する。
 モデルのEditModeテストだけでは見えない「組み合わせたときの不具合」を拾うために追加した)

現在ブランチ: `claude/smart-media-platform-phase5-3-zz8rob`(PR #3)

---

## 決定の記録(あとで「なぜこうした」を聞かれたときのため)

### フェードの秒数を3秒にした理由

`FadeOutSeconds = 3秒` / `SilentBeforeEnd = 0.8秒`(実質フェードがかかるのは残り3.8秒〜0.8秒の3秒間)。

**分かっている限界:** `GetDuration()`(動画ファイル全体の長さ)は分かっても、
**動画の中で実際に曲(音)が終わる位置**は分からない。YouTube動画は音声ファイルではなく映像作品なので、
曲が終わったあとエンドカードや無音区間が投稿者の匙加減でどれだけ付くかは動画によってバラバラ。

**それでも3秒という短さで実害が小さいと判断した理由:**
- 曲の後に長い無音・エンドカードがある動画(MV・歌ってみた系に多い)→ フェードは無音区間の中で起きる
  → 実害なし、ただ効果もない(空振り)
- 曲の後がほぼ余白の無い動画(Official Audio系)→ 本当の終わりの1〜2秒手前あたりからなめらかに下がる
  → ほぼ気づかれない。ぶった切りにはならない
- 唯一のリスクは「曲の最後の3〜4秒が、音量一定の伸ばした音(サビの一発・長い残響)で終わる曲」。
  ここは実際に下がって聞こえる可能性がある。頻度は低いと判断し、許容することにした。
- **もし他の人が `FadeOutSeconds` を大きく(10秒とか)伸ばすと、このリスクが顕在化しやすくなる。**
  Inspectorのツールチップで、伸ばしすぎるとOfficial Audio系の曲を削ってしまうリスクがあると
  まだ明記していない。追記の余地あり(未着手)。

**正確に直す方法(保留中):** 曲ごとに「フェード開始位置」をカタログにデータとして持たせる案がある。
下の「ブラウザ拡張機能」の構想に含まれる。YouTube動画をダウンロードして音声解析する案は
**却下**(YouTubeの利用規約に触れるため)。人間が聴きながらボタンを押して記録する方式を採用予定。

### プレイリストの設計

- 曲は**カタログの番号ではなくID**で覚える(カタログを作り直すと番号がずれるため)。
- 保存形式: `SMPPL1` ヘッダ + 行ごとに「名前 TAB ID TAB ID …」。壊れた行だけ捨てて残りは読む。
- お気に入り・履歴は今も**保存されない**(セッションだけ)。プレイリストだけPlayerDataに保存する。

### 対応言語の候補

英語・韓国語・繁体字中国語(簡体字より現実的、本土からのアクセス事情のため)・
ポルトガル語(ブラジル)・ロシア語・スペイン語・タイ語、あたりが候補。
確実な統計データがあるわけではなく一般論なので、実際にX投稿や類似ワールドのコミュニティの反応を見て
決めた方がいい。UIの組み込みフォント(`LegacyRuntime.ttf`)はOSフォントへのフォールバックなので、
PCなら大抵の言語がそのまま出るはずだが、**Questでの表示は未確認**(下のタスク#5と合わせて確認する)。

---

## ロードマップ / タスク

安定確認を先に、機能追加はそのあと、という順序で合意している。

### 安定確認(最優先)

- **#5 Quest実機で動作確認** — `VRChat 実機 (Unity Video)` でPrefabを作り、Questで動画が映るか・
  音が出るか・パネルが操作できるか確認。AVPro版はQuestで動かないので必ずUnity Video版で。
  **まだ試していない。** ここが一番大きい未知数(Questで動くかどうかで対象ユーザー数が大きく変わる)。
- **#6 Questで見つかった不具合を直す** — #5で症状が出たら着手。
- **#7 複数人同時使用のテスト** — 同時にNext・同時に曲を選ぶ、など操作をぶつけてみる。
- **#8 壊れたURLへの耐性を確認** — 削除済み・年齢制限のURLを混ぜて、次に進めるか。
- **#9 長時間稼働の確認** — 常設インスタンス想定で数時間つけっぱなしにする。

### 機能追加(安定確認のあと)

- **#12 ブラウザ拡張機能でカタログ作りを楽にする**(#9の直後、#10/#11より先にやる)
  YouTubeの動画ページに小さな操作を足す:
  1. 動画URLをリストに追加(ブラウザ→Unityの往復を減らす)
  2. 聴きながら「曲の終わり」の再生位置を記録 → フェード開始位置に使う
     (人間の耳で判断。ダウンロード・音声解析はしない=規約面で安全)
  3. ジャンル判定(`GenreClassifier`)の予想を見て、違っていたら選び直す
  4. タイトル自動クリーニング(`YouTubeTitleCleaner`)の結果が変なら直す
  エクスポートしたJSONをUnity側(CatalogBuilder)で読み込み、値があれば自動判定より優先する。
- **#10 お試しワールド・デモ動画の用意** — BOOTHで買う前に触れる体験ワールド。
- **#11 英語対応の検討** — まずBOOTHの商品説明を英語併記にするところから。

### 完了済み(参考)

- #1 重ねない方式のフェード
- #2 ミラー内での映像反転
- #3 名前付きプレイリスト
- #4 全検査と見直しをして納品
